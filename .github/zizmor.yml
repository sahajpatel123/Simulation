# Pinning policy, enforced by tools/validate_ci.py: EVERY action ref —
# first-party and third-party alike — is pinned to an immutable commit SHA,
# with the release version kept as a trailing comment. That is exactly the
# policy zizmor's unpinned-uses audit enforces, so no rule overrides are
# needed here anymore; the file stays as the documented home for future
# audit tuning.
rules: {}
